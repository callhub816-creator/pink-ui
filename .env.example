# Guest Chat Server Configuration
VITE_SOCKET_URL=http://localhost:3001


# AI API Configuration (Multi-key support: comma-separated)
# Keys MUST be from the SAME account for rotation/reliability.
VITE_SAMBANOVA_API_KEY=key1,key2,key3
VITE_GROQ_API_KEY=key1,key2
VITE_GEMINI_API_KEY=key1,key2

# Supabase (optional - required for persistent data/features)
# Get these from your Supabase project settings. If you don't use Supabase,
# leave these blank; the app will run in demo/guest mode but some features
# (profiles, persisted messages) will be disabled.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
